
export enum StateProviders {
    ReduxStore,
    LocalStorage,
    SessionStorage,
    Cookies
}

export enum DataProviders {
    Firebase,
    MongoDb
}

export enum StateProviderActions {
    Save,
    Remove,
    Retrieve
}
